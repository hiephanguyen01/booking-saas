/**
 * partner feature — public API
 *
 * Co-locates all partner-area domain logic.
 * Routes in `routes/partner/` import from here as the feature grows.
 *
 * Future structure:
 *   features/partner/
 *     api/          # partner-specific API calls (revenue, listings, bookings)
 *     components/   # CalendarView, RevenueChart, etc.
 *     hooks/
 *     loader.ts
 *     action.ts
 *     index.ts
 */

// placeholder — add exports as partner logic is extracted from routes/partner/
export {};
