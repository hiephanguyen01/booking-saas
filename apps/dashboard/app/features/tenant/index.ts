/**
 * tenant feature — public API
 *
 * Co-locates all tenant-area domain logic.
 * Routes in `routes/tenant/` import from here as the feature grows.
 *
 * Future structure:
 *   features/tenant/
 *     api/          # tenant-specific API calls
 *     components/   # ListingForm, BookingTable, etc.
 *     hooks/
 *     loader.ts
 *     action.ts
 *     index.ts
 */

// placeholder — add exports as tenant logic is extracted from routes/tenant/
export {};
