# Review package: d66a452e1d8690586cb69bc06a7c1438df95eebe..0711c3b

## Commits
0711c3b fix(storefront): support same-day search ranges

## Files changed
 apps/storefront/app/features/search/search-form.tsx | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

## Diff
diff --git a/apps/storefront/app/features/search/search-form.tsx b/apps/storefront/app/features/search/search-form.tsx
index edd0549..82e446a 100644
--- a/apps/storefront/app/features/search/search-form.tsx
+++ b/apps/storefront/app/features/search/search-form.tsx
@@ -650,21 +650,21 @@ function SearchDatePicker({
         <Calendar
           mode="range"
           selected={range}
           onSelect={(next) => {
             const selected = next ?? { from: undefined };
             setRange(selected);
             if (selected.from && selected.to) close();
           }}
           disabled={calendarToday ? { before: calendarToday } : undefined}
           numberOfMonths={months}
-          resetOnSelect={mode === 'inventory'}
+          resetOnSelect
           formatters={formatters}
           className="sf-calendar w-full [--cell-size:2.25rem]"
         />
       );
 
     return (
       <div className="flex flex-col">
         {showModeTabs && availableModes.length > 1 ? (
           <ModeToggle
             mode={mode}
