# Use cases written before ADR 0009, still without a unit test.
#
# This list is DEBT, not an exemption policy. It may only shrink:
#   - write the test beside the use case, then delete its line here;
#   - lower BACKFILL_CEILING in use-case-unit-tests.test.ts to match.
# A NEW use case never belongs here — it ships with its test or it does not merge.
#
# Touching one of these? Backfill it in the same change. That is the whole rule.
apps/api/src/modules/catalog/application/use-cases/list-nearby-public-listings.use-case.ts
apps/api/src/modules/catalog/application/use-cases/search-public-catalog.use-case.ts
apps/api/src/modules/catalog/application/use-cases/update-listing-type.use-case.ts
apps/api/src/modules/scheduling/application/use-cases/get-availability.use-case.ts
